#!/usr/bin/expect -f

set timeout 60
set password {$$R00tbeer02}

# Copy reset script
spawn scp /tmp/reset_commands.sh root@155.138.197.128:/tmp/reset_commands.sh
expect {
    "(yes/no)?" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "$password\r"
    }
}
expect eof

# Execute reset
spawn ssh root@155.138.197.128 "chmod +x /tmp/reset_commands.sh && /tmp/reset_commands.sh"
expect {
    "password:" {
        send "$password\r"
    }
}
expect eof