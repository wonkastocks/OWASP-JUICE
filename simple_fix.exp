#!/usr/bin/expect -f

set timeout 30
set password {$$R00tbeer02}

spawn ssh root@155.138.197.128

expect {
    "password:" {
        send "$password\r"
    }
    "yes/no" {
        send "yes\r"
        expect "password:"
        send "$password\r"
    }
}

expect "# "

# Simple sed commands
send "sed -i 's/Login Demo/Login/g' /var/www/html/login.php\r"
expect "# "

send "systemctl restart apache2\r"
expect "# "

send "exit\r"
expect eof

puts "\nFixed!"