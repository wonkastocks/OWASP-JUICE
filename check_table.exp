#!/usr/bin/expect -f

set timeout 30
set password {$$R00tbeer02}

spawn ssh root@155.138.197.128

expect {
    "password:" {
        send "$password\r"
    }
    "yes/no" {
        send "yes\r"
        expect "password:"
        send "$password\r"
    }
}

expect "# "

# Check table structure
send "mysql -u root wallys_monkey_parts -e \"DESCRIBE users;\"\r"
expect "# "

# Check users
send "mysql -u root wallys_monkey_parts -e \"SELECT id, username, email, is_admin FROM users;\"\r"
expect "# "

send "exit\r"
expect eof